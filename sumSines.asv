function outputWaveMesh = sumSines(x,t,w_x,w_t_range)

[X,T] = meshgrid(x,t);

wavemesh = zeros(size([X,T]));

% outputWave = zeros[
% for w = k_cosineRange
% sum(cos(k_wavenumber*x-w


for w_t = w_t_range
    wavemesh = wavemesh + cos(w_x*X - w_t*T);
end

outputWaveMesh = wavemesh;

end